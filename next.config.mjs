/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["localhost", "images.unsplash.com", "platinumlist.net", "www.pharmica.co.uk"], // Allow images from localhost & Unsplash
  },
};

export default nextConfig;
